"""Optimizer implementations — import via the full scenario path, e.g.
``from antomnievo.optimizer.text2sql.dp_text2sql_optimizer import DPText2SQLOptimizer``.
Kept free of eager imports so one scenario does not pull in another's dependencies."""
