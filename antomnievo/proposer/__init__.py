"""Proposer implementations — import via the full path, e.g.
``from antomnievo.proposer.pi_coding_agent_proposer import PiCodingAgentProposer``.
Kept free of eager imports so one backend does not pull in another's dependencies."""
