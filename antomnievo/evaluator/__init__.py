"""Evaluator implementations — import via the full scenario path, e.g.
``from antomnievo.evaluator.text2sql.text2sql_evaluator import Text2SQLEvaluator``.
Kept free of eager imports so one scenario does not pull in another's dependencies."""
